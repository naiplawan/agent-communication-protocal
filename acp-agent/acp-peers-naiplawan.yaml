config_version: 1

this_agent:
  agent_id: naiplawan-agent
  machine_id: naiplawan-machine
  http_endpoint: http://localhost:8444
  capabilities:
    - agent
    - signaling

peers:
  - agent_id: acp-relay
    machine_id: relay
    http_endpoint: http://localhost:8443
    capabilities:
      - relay
      - registry

security:
  default_auth_type: signed-token
  token_ttl_seconds: 3600
  require_https: false
